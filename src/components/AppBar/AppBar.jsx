import { Header } from "./AppBar.styled";
import Navigation from "components/Navigation";

const AppBar = () => {
  return (
    <Header>
      <Navigation />
    </Header>
  );
};

export default AppBar;