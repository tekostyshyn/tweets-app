import styled from 'styled-components';

export const List = styled.ul`
  width: calc(380px * 3 + 48px * 2);
  padding-left: 0;
  list-style: none;
  display: flex;
  flex-wrap: wrap;
  gap: 48px;
`;
